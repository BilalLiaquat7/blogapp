<div class="user_show container">

  <div class="user_item row">
    <div class="u_img">
      <img src= "<%= @set_user.photo %>" alt="User Image">
    </div>

    <div class="user_cont">
      <a href=<%= user_url(@set_user.id) %>>
        <div class="user_desc col">
          <h2 class="user_name"><%= @set_user.name %></h2>
          <p class="user_post_n">Number of posts: <%= @set_user.postsCounter %></p>  
        </div>
      </a>
    </div> 
  </div>

  <div class="user_bio">
    <p class="show_title">Bio</p>
    <p class="bio_detail"><%= @set_user.bio %></p>
  </div>

  <ul class="post_list col">
    <% @recent_posts.each do |post| %>
      <a href=<%= user_post_url(user_id: @set_user.id, id: post.id) %>>

        <li class="post_item">
          <h4 class="post_title"><%= post.title %></h4>
          <p class="post_text"><%= post.text %></p>
          <p class="ikes">
            Comments: <%= post.commentssCounter %>,
            Likes: <%= post.likesCounter %>
          </p>
        </li>

      </a>
    <% end %>
  </ul>
  <div class="btn_cont">
  <a href=<%= user_posts_url(user_id: @set_user.id) %>>
    <button class="btn">See all posts</button>
  </a>
</div>

</div>